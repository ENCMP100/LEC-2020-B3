function [firstname, lastname] = getname
% GETNAME prompts for the first and last name and returns
% the values entered by the user.

firstname = input('Enter the first name: ', 's');
lastname = input('Enter the last name: ', 's');

end
